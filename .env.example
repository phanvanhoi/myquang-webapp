# Copy thành .env trên VPS: cp .env.example .env
# Sinh secret: openssl rand -hex 32

SECRET_KEY=change-me-to-a-long-random-string

# Chỉ bật khi chạy HTTPS qua nginx/reverse proxy:
# TRUST_PROXY=1
# COOKIE_SECURE=1
