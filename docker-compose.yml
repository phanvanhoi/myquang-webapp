services:
  myquang:
    build: .
    container_name: myquang-app
    ports:
      - "8090:8000"
    volumes:
      - myquang_data:/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=8000
      - DB_PATH=/data/myquang.db

volumes:
  myquang_data:
