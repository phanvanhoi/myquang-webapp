services:
  myquang:
    build: .
    container_name: myquang-app
    network_mode: host
    volumes:
      - myquang_data:/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DB_PATH=/data/myquang.db

volumes:
  myquang_data:
